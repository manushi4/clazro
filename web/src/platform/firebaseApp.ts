// Firebase Web SDK can be added here if needed
// For now, providing no-op implementation

export const initializeApp = () => ({});
export const getApp = () => ({});

export default {
  initializeApp,
  getApp,
};
